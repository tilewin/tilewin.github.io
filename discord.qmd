---
title: "Discord"
subtitle: "A metadata analysis of one discord server"
format: 
  html:
    code-fold: true
editor: visual
---

## Quarto

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

## Running Code

When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```{r}
1 + 1
```

You can add options to executable code like this

```{r}
#| echo: false
#| warning: false
#| 
library(shiny)
library(bslib)
library(tidyverse)
library(glue)
library(ggridges)
library(hms)

setwd("C:/Users/Tom/Documents/discoRd")

# Setup -------------------------------------------------------------------

df <- read_csv(
    file = "./data/logs.csv", 
    col_select = c(content, date), 
    show_col_types = FALSE
  ) |>
  mutate(
    dttm = date,
    time = as_hms(date),
    date = as.Date(dttm)
  )

theme_set(theme_bw(base_size = 16))


df |>
  select(content, date) |> 
  distinct() |>
  ggplot(aes(x = date)) + geom_bar()

```

@fig-timeline is the figure name

```{r}
#| label: fig-timeline
#| fig-cap: "Joining time"
#| echo: false
#| warning: false

df |>
  ggplot(aes(x = time)) + geom_density()
```
